name: Deploy a Netlify (legacy — auto-deploy via GitHub connection active)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Notify
        run: echo "Auto-deploy desde GitHub ya activo. Este workflow ya no se ejecuta en push."
